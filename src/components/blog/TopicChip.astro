---
interface Props {
  topic: string;
  class?: string;
}

const { topic, class: className } = Astro.props;
const label = topic.replace(/-/g, ' ');
---

<a
  href={`/blog/topics/${topic}`}
  class:list={[
    'inline-flex w-fit items-center rounded-chip bg-mint-100 px-3 py-1 font-mono text-xs font-medium text-mint-700 capitalize hover:brightness-95',
    className,
  ]}
>
  {label}
</a>
